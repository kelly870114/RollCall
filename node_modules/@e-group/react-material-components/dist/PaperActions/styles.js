var styles = function styles(theme) {
  return {
    root: {
      padding: "".concat(theme.spacing.unit * 3, "px"),
      paddingTop: 0,
      '& button': {
        margin: "0 ".concat(theme.spacing.unit * 0.5, "px")
      }
    },
    right: {
      textAlign: 'right'
    },
    left: {
      textAlign: 'left'
    },
    center: {
      textAlign: 'center'
    }
  };
};

export default styles;