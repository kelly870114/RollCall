var styles = function styles(theme) {
  return {
    '@global': {
      html: {
        height: '100%'
      },
      body: {
        height: '100%'
      }
    },
    root: {
      height: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    },
    container: {
      textAlign: 'center'
    }
  };
};

export default styles;