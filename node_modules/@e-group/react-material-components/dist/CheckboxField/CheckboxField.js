import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import React from 'react';
import PropTypes from 'prop-types';
import { fieldInputPropTypes, fieldMetaPropTypes } from 'redux-form';
import Checkbox from '../Checkbox';

var CheckboxField = function CheckboxField(_ref) {
  var _ref$input = _ref.input,
      value = _ref$input.value,
      otherInput = _objectWithoutProperties(_ref$input, ["value"]),
      meta = _ref.meta,
      MUICheckboxProps = _ref.MUICheckboxProps,
      other = _objectWithoutProperties(_ref, ["input", "meta", "MUICheckboxProps"]);

  return React.createElement(Checkbox, _extends({
    MUICheckboxProps: MUICheckboxProps,
    checked: value
  }, otherInput, other));
};

CheckboxField.propTypes = {
  // redux form props
  input: PropTypes.shape(fieldInputPropTypes).isRequired,
  meta: PropTypes.shape(fieldMetaPropTypes).isRequired,
  // customize props
  MUICheckboxProps: PropTypes.object
};
export default CheckboxField;