import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import React from 'react';
import classNames from 'classnames';
import withStyles from '@material-ui/core/styles/withStyles';
import styles from './styles';

var Main = function Main(_ref) {
  var classes = _ref.classes,
      className = _ref.className,
      other = _objectWithoutProperties(_ref, ["classes", "className"]);

  console.warn("Main is been deprecated please use new component Container instead. It'll be removed in next major release.");
  return React.createElement("main", _extends({
    className: classNames(classes.root, className)
  }, other));
};

export default withStyles(styles)(Main);